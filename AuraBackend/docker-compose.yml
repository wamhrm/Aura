services:
  app:
    build: .
    ports:
      - "8080:8080"
    env_file: .env
    environment:
      PORT: "8080"

  migrate:
    build: .
    env_file: .env
    command: ["migrate", "--yes"]
    profiles:
      - migrate
